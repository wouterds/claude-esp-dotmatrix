export { BLACK, type Color, gammaCorrect, hex, hsv, lerp, rgb, scale } from "./color";
export { findBoard, type Matrix, type MatrixOptions, openMatrix } from "./device";
export { createFrame, type Frame, isRotation, ROTATIONS, type Rotation } from "./frame";
export {
  DEFAULT_BRIGHTNESS,
  FRAME_BYTES,
  HEIGHT,
  LED_COUNT,
  MAX_BRIGHTNESS,
  WIDTH,
} from "./protocol";
